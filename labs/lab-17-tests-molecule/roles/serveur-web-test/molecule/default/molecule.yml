---
dependency:
  name: galaxy

driver:
  name: docker

platforms:
  - name: instance-debian
    image: debian:bookworm
    pre_build_image: true

provisioner:
  name: ansible

verifier:
  name: ansible
